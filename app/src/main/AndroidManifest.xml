<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.netbucket.studymate">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".activities.UserApprovalPendingActivity" />
        <activity android:name=".activities.FacultyRegistrationStepThreeActivity" />
        <activity android:name=".activities.StudentRegistrationStepThreeActivity" />
        <activity android:name=".activities.AdminRegistrationStepThreeActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.StudentDashboardActivity" />
        <activity android:name=".activities.FacultyDashboardActivity" />
        <activity android:name=".activities.AdminDashboardActivity" />
        <activity
            android:name=".activities.OnBoardingActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.MembersActivity" />
        <activity android:name=".activities.InfoActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.FacultyRegistrationStepTwoActivity" />
        <activity android:name=".activities.AdminRegistrationStepTwoActivity" />
        <activity android:name=".activities.PasswordResetActivity" />
        <activity android:name=".activities.StudentRegistrationStepTwoActivity" />
        <activity android:name=".activities.CommonRegistrationStepOneActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/> <!-- optional (needed if default theme has no action bar) -->
    </application>

</manifest>